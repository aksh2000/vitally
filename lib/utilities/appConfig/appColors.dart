import 'package:flutter/material.dart';

class AppColors {
  // This class contains the App Color constants based on UI
  // Making changes to the constants will reflect on the entire app

  final Color blue = Color(0xFF5433FF);
  final Color green = Color(0xFF00CDAC);
  final Color grey = Color(0xFFC4C4C4);
  final Color lightGrey = Color(0xFFEEEEEF);
  final Color skyBlue = Color(0xFF33B6FF);
  final Color orange = Color(0xFFFF7A00);
  final Color white = Colors.white;
  final Color black = Colors.black;
  final Color red = Colors.red;
}
