// This file contains the declaration of all Enums used in this project
// Review before making any changes to the existing enums

enum Gender { male, female }
enum DailyActivity { sedentary, lightlyActive, moderatelyActive, veryActive }
enum Goal { beHealthier, loseWeight, gainWeight }
enum GoalPageState { selectingGoal, customizingTargetWeight }
enum DashBoardScreen { home, stats, healthLog, profile }
